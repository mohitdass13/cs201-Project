#include<stdio.h>
#include<stdlib.h>    //for using the malloc function
struct dictionary{
    char word[30];     // array to stire the word of maximum of 20 letters
    char meaning[30];  //  array to store the simple meaning of word  30 letters 
    struct dictionary* left;   
    struct dictionary* right;
};
struct dictionary* root=NULL;   // root node is null as the dictionary will be empty after creating. we have to add words into it

int check(char a1[],char a2[])  // function to check whether the word a1 is greater or smaller or smaller than a2.
{
    int i,c;
    for(i=0;a1[i]!='/0')
    {
      i=i+1;
    }
    int j;
    for(j=0;a1[j]!='/0')
    {
      j=j+1;
    }
    if(i>j)
    c=1;
    else if(i<j)
    c=-1;
    else
    {
        for(i=0;i!='/0';i++)
        {
            if(a1[i]>a2[i]){
            c=1;
            break;}
            else if(a1[i<a2[i]]){
            c=-1;
            break;
            }
            else
            {
                c=0;
            }
        }
        if(c==1)
        return 1;
        else if(c==-1)
        return -1;
        else 
        return 0;
    }
} 
